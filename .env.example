# Server-only. Never expose this value through a NEXT_PUBLIC_ variable.
OPENAI_API_KEY=

# Centralized model alias. Override only when product owners approve a newer GPT-5.6 production alias.
OPENAI_MODEL=gpt-5.6
